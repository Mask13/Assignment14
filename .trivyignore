# .trivyignore
# Temporary Trivy ignores for known/accepted issues while a proper upgrade is scheduled.
# Each line is a CVE id or a glob path to ignore inside the scanned image.
# TODO: Replace this ignore by upgrading the package(s) and removing the corresponding lines.

# Ignore specific vulnerabilities by CVE ID
CVE-2025-62727  # starlette: DoS via Range header merging (fixed in 0.49.1)
CVE-2024-33663  # python-jose: algorithm confusion with OpenSSH ECDSA keys (fixed in 3.4.0)
CVE-2025-43859  # h11: accepts malformed Chunked-Encoding bodies (fixed in 0.16.0)

# If you have specific CVE ids (preferred), list them instead of file paths, e.g.:
# CVE-2025-XXXXXX
